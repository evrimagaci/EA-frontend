import { RouterLowercasingPipe } from './router-lowercasing.pipe';

describe('RouterLowercasingPipe', () => {
  it('create an instance', () => {
    const pipe = new RouterLowercasingPipe();
    expect(pipe).toBeTruthy();
  });
});
